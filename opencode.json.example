{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "sqlmcp": {
      "type": "local",
      "command": ["/absolute/path/to/sqlmcp", "-driver", "postgres", "-dsn", "postgres://user:password@localhost:5432/mydb?sslmode=disable"],
      "enabled": true
    }
  }
}
